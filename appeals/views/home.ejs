<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Atlas Networks Appeal System</title>
  <!-- Bootstrap 5 CSS -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css"
    rel="stylesheet"
  >
  <!-- Playful Font: Poppins -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
  <!-- Override Bootstrap primary color and add modern styling -->
  <style>
    :root {
      --bs-primary: #343a40;
      --bs-primary-rgb: 52, 58, 64;
    }
    body {
      font-family: 'Poppins', sans-serif;
    }
    /* Enlarged Navbar with rounded edges */
    .navbar {
      padding: 1.5rem 1rem;
      border-bottom-left-radius: 0 0 1rem 1rem;
      border-bottom-right-radius: 0 0 1rem 1rem;
    }
    .navbar-brand {
      font-size: 1.75rem;
      font-weight: 600;
    }
    .nav-link {
      font-size: 1.1rem;
      margin-left: 1rem;
    }
    /* Card styling */
    .card {
      border-radius: 1rem;
      border: none;
    }
    .btn-discord {
      background-color: #5865F2;
      color: #ffffff;
      border-radius: 2rem;
      padding: 0.75rem 1.5rem;
      font-weight: 500;
      transition: background-color 0.3s ease, transform 0.2s ease;
    }
    .btn-discord:hover,
    .btn-discord:focus {
      background-color: #4752c4;
      transform: scale(1.05);
    }
    .btn-discord .icon {
      filter: brightness(0) invert(1);
    }
    /* Fade-in animation for hero card */
    .fade-in {
      animation: fadeIn 1s ease-in-out;
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body class="bg-dark text-light d-flex flex-column min-vh-100">

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-lg">
    <div class="container">
      <a class="navbar-brand" href="#">Atlas Networks</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto align-items-center">
          <li class="nav-item"><a class="nav-link active" href="#">Home</a></li>
          <li class="nav-item"><a class="nav-link" href="#about">About</a></li>
          <li class="nav-item"><a class="nav-link" href="#contact">Contact</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Hero Section -->
  <main class="container flex-fill d-flex align-items-center justify-content-center">
    <div class="card bg-secondary text-light shadow-lg fade-in" style="max-width: 28rem;">
      <div class="card-body text-center">
        <h2 class="card-title mb-3">Welcome</h2>
        <p class="card-text mb-4">Sign in with your Discord account to manage appeals.</p>
        <a href="/login" class="btn btn-discord btn-lg d-flex align-items-center justify-content-center mx-auto">
          <img
            src="https://cdn.jsdelivr.net/npm/simple-icons@v8/icons/discord.svg"
            alt="Discord Logo"
            width="24" height="24"
            class="icon me-2"
          >
          Sign in with Discord
        </a>
      </div>
    </div>
  </main>

  <!-- Footer -->
  <footer class="bg-primary text-center text-white py-3 mt-auto rounded-top">
    <div class="container">
      <small>&copy; 2025 Atlas Networks. All rights reserved.</small>
    </div>
  </footer>

  <!-- Bootstrap JS Bundle (includes Popper) -->
  <script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.bundle.min.js"
  ></script>
</body>
</html>
